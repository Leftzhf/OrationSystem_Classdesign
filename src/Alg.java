import java.util.LinkedList;

public class Alg implements Dispatch{
    BasicUtil basicUtil = new BasicUtil();
    @Override
    public void FcFs(LinkedList<ProcessStructure> queue, String currentTime) {
        Time current = basicUtil.formatTime(currentTime);
    }

    @Override
    public void SjF(LinkedList<ProcessStructure> queue, String currentTime) {
        Time current = basicUtil.formatTime(currentTime);
    }

    @Override
    public void HrrN(LinkedList<ProcessStructure> queue, String currentTime) {
        Time current = basicUtil.formatTime(currentTime);
    }

    @Override
    public void HpF(LinkedList<ProcessStructure> queue, String currentTime) {
        Time current = basicUtil.formatTime(currentTime);
    }

    @Override
    public void SRTF(LinkedList<ProcessStructure> queue, String currentTime) {
        Time current = basicUtil.formatTime(currentTime);
    }
}
